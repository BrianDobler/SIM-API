describe('distributions', () => {

    it('return a normal distribution array', () => {
    });

    it('return an exponential distribution array', () => {
    });

    it('return an uniform distribution array', () => {
    });

});